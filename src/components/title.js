import { Typography } from "@mui/material";
const Title = ({ title }) => {
    return (
        <Typography variant="h4" gutterBottom>
            {title}
        </Typography>
    );
}
export default Title